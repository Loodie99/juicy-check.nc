services:
  juicy-check:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elodietran-frontend
    restart: unless-stopped
    ports:
      - "5070:5070"
    environment:
      - NODE_ENV=production

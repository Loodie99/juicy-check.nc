services:
  juicy-check:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elodietran-frontend
    restart: unless-stopped
    ports:
      - "5070:5070"
    environment:
      - NODE_ENV=production
      - VITE_BACKEND_URL=http://15.235.206.148:5007
